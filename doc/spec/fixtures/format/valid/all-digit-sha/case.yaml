spec-version: 1
description: an all-digit commit SHA survives YAML's number resolution, so D6 still matches it
init: {from: tree, date: 2026-08-05T00:00:00Z}
run: {command: [doctor, --json]}
expect: {exit: 0, diagnostics: []}
