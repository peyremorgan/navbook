node_modules/
dist
*.tgz
.DS_Store

# Nuxt's build artefacts and the marker its test module writes.
.nuxt/
.output/
packages/web/.nuxtrc
packages/web/test-results/
packages/web/playwright-report/

# What one deployment differs from another by: hostnames, an issuer, a token.
# `.env.example` is the committed one, and names every key.
.env
