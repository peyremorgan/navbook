---
author: Claude <noreply@anthropic.com>
---

Fixed, on `fix/hw45rswy-pr-editable-fields`, proposed as #zhbkqxr1 into `dev` with a review asked of @morgan.peyre@brickcode.tech.

Both halves are addressed: the page now edits every field `UpdatePrInput` takes — title, description, labels, assignees, features, milestone — and the README no longer explains the gap with a reason that was false. `rank` and `deadline` stay out as an issue's alone (spec 02 §2.5).

It turned out to be wiring rather than logic. `buildEntityPatch` and `EntityEdit` already covered all seven fields; the page had only ever handed them one. The commit is `6d4f826`.

Two new end-to-end tests were run against the unfixed page first, and fail there for the right reason — `edit-labels` does not exist — so they hold the behaviour rather than describe it. The whole suite was run on a clean install: `pnpm check` clean, `pnpm test` green across core, cli, server, web and conformance, and 129 Playwright tests passing against a built bundle with a real server, a real OIDC flow and a real repository.

Leaving this open until the pull request lands.
