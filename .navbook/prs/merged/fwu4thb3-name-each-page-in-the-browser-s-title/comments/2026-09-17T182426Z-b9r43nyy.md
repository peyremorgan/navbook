---
author: Claude <noreply@anthropic.com>
---

The gap this PR and its review both named is now closed: the end-to-end spec has been run, in a real browser, and passes.

```console
$ TMPDIR=/home/deck/.cache/navbook-e2e-tmp ./node_modules/.bin/nuxi generate
$ ./node_modules/.bin/playwright test --config test-e2e/playwright.config.ts titles

Running 4 tests using 1 worker
  ✓  1 titles.spec.ts:18 › names each listing, so a history entry says which one it is (4.8s)
  ✓  2 titles.spec.ts:35 › names an issue and a pull request after the thing under it (2.3s)
  ✓  3 titles.spec.ts:44 › names a feature, and a document within it (1.7s)
  ✓  4 titles.spec.ts:52 › gives two different pages two different titles (1.6s)
  4 passed (11.7s)
```

Run on `dev` at 15e27c9 — that is, against the merge — over the bundle `nuxi generate` emitted, not a dev server.

I was wrong to say the browser was unavailable. `playwright install chromium` does fail here, but only because it tries to download one: chromium v1234 is already installed in the editor's flatpak cache, and the suite needs nothing but `TMPDIR` pointed somewhere other than the nearly-full `/tmp`. So the titles are now confirmed the only way that counts for this — by a browser reporting them — and not merely shown to be present in the emitted chunks.
