---
author: Claude <noreply@anthropic.com>
verdict: approve
revision: 6d4f8267ffa48ee0dece19e775274e74c552d0f1
---

Approved, on revision `6d4f826`, after reading the diff against the design in #hw45rswy's plan comment and running everything in a fresh worktree of this branch (a checkout the other sessions on this machine could not move from under the run).

**Code.** Matches the design point for point. `save(change, wrote)` keeps the one-field-at-a-time rule and the two early returns (a `PatchError` becomes a toast, a null patch is not sent). The four `LabelEditor`s and two `EditableText`s take the same props the issue page passes; the Assignees display slot says "None" itself, which it must since a slot suppresses the component's own empty text. `EditableText`'s new `disabled` is the one prop the components lacked, and a `PRECONDITION` refusal now withdraws all six editors. The suggestion listing is `{ filter: {}, allRefs: false }`, which is byte-for-byte what a bare `/prs` sends (`toEntityFilter` drops every empty key), so the cache-sharing claim in the comment holds. `rank` and `deadline` are correctly absent. No server change, and none was warranted.

**Docs.** The README paragraph now separates what the API does not expose from what the client does, and names `nav pr update` as appending a revision so the verb stops reading as the ordinary word. Spec `client.md` and the page header agree with it.

**Runs**, on this branch at `6d4f826`, dependencies installed from the lockfile:

- `pnpm check`: clean.
- `pnpm test`: core 682, web 289, server 248, cli 302, conformance 114, all passing.
- `pnpm --filter @navbook/web build && test:e2e`: 129 passed in 5.9 minutes, against the built bundle with a real server, OIDC flow and repository. That includes `edits the fields updatePr takes, not only the reviewers`, `edits a pull request's title in place`, and the widened `refuses to change the reviewers of a branch it does not serve`, which now asserts every `edit-*` control is gone.

For contrast, the same suite on `dev` without this branch is 127 tests and passes too, which is the point made in the issue: nothing on `dev` asserted the fields were read-only, so the two new tests are what hold the behaviour from here.

**Merge.** `git merge-tree --write-tree dev fix/hw45rswy-pr-editable-fields` is clean against `dev` at `08ef78b`, which has since taken #oifeg19c and a run of docs commits, none touching `packages/web`. No rebase is needed, and none is done, so the approval stays bound to the revision that was tested.

Not blocking, for a follow-up if anyone wants it: milestone suggestions are drawn from pull requests only, and an issue listing could be merged in since milestones usually span both.
