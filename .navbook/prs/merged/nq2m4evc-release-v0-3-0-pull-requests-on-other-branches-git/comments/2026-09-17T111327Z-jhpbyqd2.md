---
author: Morgan PEYRE <morgan.peyre@brickcode.tech>
verdict: approve
revision: b0aa45aadc817e1f7fa245aa14a88044b90682c1
---

Self-review of the release pull request. The code in it was reviewed in #z3j95v3e (and, for the rest of 0.3.0, in #f2khcfyx); this review checks that dev is releasable and that the notes match what ships.

**Verified locally on dev at b0aa45a** (Node 24.21, macOS):

- `pnpm check` (biome and typecheck) and `pnpm -r --if-present codegen:check` pass.
- `pnpm test`: core 699, cli 331, server 286, web 307 (vitest), conformance 115 and deploy 63, with no failures.
- `pnpm build`, then the conformance suite against the built CLI (`NAV_BIN=node packages/cli/dist/main.js`): 115 pass.
- Tarballs packed with the pinned pnpm 11.18.0, then installed into an empty project: `nav --version` prints 0.3.0, `nav init`, `nav issue open` and `nav doctor` succeed, and `nav-server --help` resolves. The packed CLI depends on `@navbook/core` 0.3.0.
- The core, cli and server versions are 0.3.0, so they match the `v0.3.0` tag `release.yml` checks.

**Checked against the diff (`main..dev`, 13 files):** the "Pull requests on other branches" section and the two new Upgrading entries match commits 5ad20ee through 217880f. `assertOnBranch` runs before any file is written, and the server's `pr` query now calls `readPr`.

**Not verified here:**

- The Playwright suite, which needs a browser install. CI's `web-e2e` job runs it on the push to `main`, and the tag will wait for that run to pass.
- Docker images, since there is no local engine.

**Worth knowing:** a globally installed pnpm 9 ignores `packageManager` and rejects `pnpm --filter … pack`. The release workflow's pnpm/action-setup installs 11.18.0, so this affects only local runs.
