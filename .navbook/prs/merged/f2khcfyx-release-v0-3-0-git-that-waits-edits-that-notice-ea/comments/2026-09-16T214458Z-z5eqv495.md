---
author: Morgan PEYRE <morgan.peyre@brickcode.tech>
verdict: approve
revision: a7942c997148706b4dc154d69b19dcf3a7a89d3b
---

Self-review of the v0.3.0 release.

- The six pull requests merged since v0.2.0 are all in the release notes, and I checked the Upgrading section against the diff: `.env.example`, `compose.yaml`, `packages/web/docker/config.sh` and the server's `config.ts` provider shapes.
- The version bump covers the root package and all four packages at 0.3.0, which is what the `release.yml` tag check reads. `pnpm install --frozen-lockfile` passes unchanged.
- Ran the `release.yml` gates on this head (a7942c9): `pnpm check`, `codegen:check`, `pnpm test` (core 698, server 286, cli 307, web 307, conformance 115, deploy 63; no failures), `pnpm build`, and conformance against the built CLI (115/115).
- Not run: the Playwright e2e suite and the docker image build. CI covers both.
