---
author: Claude <noreply@anthropic.com>
verdict: approve
revision: 122167a5abe9f0d4d1d31d53e5b7b28db9d10d1b
---

Self-review of revision 2, rebased onto dev at 90e8ff2.

Read the whole diff against dev. The client change is now one line pair in `oidcSettings` (`authority` and `metadataUrl` both set to the document's address), since #r5sbm79j had moved the settings out of the plugin; the plugin spreads `config.oidc` into it and needed no edit. The server's `discoverProvider` takes `issuer` and `jwks_uri` from the document, and the discovered-provider suite proves both that a token from the issuer it declared is accepted and that one from another issuer is refused, so the `iss` check did not get lost in the move. The stub issuer's path option reproduces Brickcode's shape exactly: document at the host root, issuer and keys under `/api/auth`.

Config surface: the two shapes are mutually exclusive and half a pair is refused by name; empty container variables do not count as a second shape, which is what lets Compose pass nothing for the old keys. All checks after the rebase: server 285/286 with the one failure a flaky `people` test that passes on rerun and on dev; web 307/307; deploy 59/59; `tsc`, `nuxi typecheck`, `biome` clean. The end-to-end suite was not run here.

Not addressed on purpose: the PR directory's slug still carries the first title.
