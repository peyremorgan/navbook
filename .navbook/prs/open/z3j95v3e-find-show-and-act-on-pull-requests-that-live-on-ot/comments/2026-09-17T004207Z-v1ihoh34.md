---
author: Morgan PEYRE <morgan.peyre@brickcode.tech>
verdict: approve
revision: 2d247259d7b18fdbdab6c1e8a8a3f5047f4bdf8a
---

Self-review of revision 3 (`2d24725`), against #t4mwvm2j and #mdftn010.

**The fix works.** I ran the same scenario against a build of `dev` and a build of this branch: a throwaway repository with a pull request on `feat/x`, and commands run from `main`.

- On `dev`: `pr show` fails with "no pull request matches" for the bare ID, for `#<id>` and for the `--json` path, and so do `review` and `comment`. `review --commit` from a detached HEAD commits the review onto no branch.
- On this branch: `show` reads the pull request from `feat/x` and says so on stderr. `review` and `comment` exit 1, write nothing, and name the branch to switch to, or the worktree when another one has it checked out. `--commit` on a detached HEAD is refused before any file is written. An empty `pr list` shows the stderr hint pointing to `--all-refs`.

**Checks.** At the branch head, `pnpm check` is clean, and all suites pass: core 683, cli 326, server 248, conformance 114. `pnpm check` also passes on each code commit on its own. I didn't run the test suites commit by commit. Revision 3 is revision 2 without the integration guide, which is now #kl6ebnrs, so the code is unchanged. `nav doctor` reports no errors.

**Spec.** `doc/spec/04-cli.md` describes each change: the new ID forms, the `--commit` refusal on a detached HEAD, the hint, and the read fallback with write refusal.

**Review feedback.** Findings 1–3 of puwzd1m3 are fixed. Finding 4 does not apply: `nextState` in `core/review.ts` keeps an earlier opinion when a `comment` verdict follows, and existing tests cover that.

**Merging.** This conflicts with `dev` in `packages/core/src/ops/pr.ts` only. `dev` has since added `worktreeHolding`, which is identical to `worktreeOfBranch` here. The merge will keep `dev`'s helper and drop this one.

**Left for later**, as the description says: `show` from a third branch can display an out-of-date "open" copy of a pull request its target has already merged.
